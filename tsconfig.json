{
  "include": ["client/src/**/*", "shared/**/*", "server/**/*"],
  "exclude": ["node_modules", "build", "dist", "**/*.test.ts"],
  "compilerOptions": {
    /* Build settings */
    "incremental": true,
    "tsBuildInfoFile": "./node_modules/typescript/tsbuildinfo",
    "noEmit": true,

    /* Module system */
    //"module": "ESNext",
    "moduleResolution": "node16",   // ✅ fixes require/import mixing
    "module":"commonjs",
    "allowImportingTsExtensions": true,

    /* Language features */
    "target": "ESNext",
    "lib": ["esnext", "dom", "dom.iterable"],
    "jsx": "preserve",
    "strict": true,

    /* Interop */
    "esModuleInterop": true,
    "allowSyntheticDefaultImports": true,

    /* Performance & safety */
    "skipLibCheck": true,

    /* Paths / Aliases */
    "baseUrl": ".",
    "paths": {
      "@/*": ["./client/src/*"],
      "@shared/*": ["./shared/*"]
    },

    /* Types */
    "types": ["node", "vite/client"]
  }
}
